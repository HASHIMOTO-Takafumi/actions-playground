name: Foo
on:
  push:
    branches:
      - trigger-test

jobs:
  test:
    name: bar
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: set env
        run: |
          OSVER=$(cat /etc/os-release)
          echo $OSVER
