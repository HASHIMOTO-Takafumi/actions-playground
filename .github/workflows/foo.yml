name: Foo
on:
  push:
    branches:
      - trigger-test

jobs:
  test:
    name: bar
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.5.2'
      - run: go version
